# 汪总石材 · 线上展厅部署指南

## 📁 文件结构

```
wang-stone/
├── index.html          ← 主页面（唯一需要的文件）
└── images/
    ├── villa-33/       ← 33# 改造前后对比
    ├── villa-77/       ← 77# 改造前后对比
    ├── villa-86/       ← 86# 改造前后对比
    ├── villa-112/      ← 112# 改造前后对比
    ├── craft/          ← 法式葡米工艺细节
    └── factory/        ← 自有工厂实拍
```

## 🚀 部署方案（免费/低成本）

### 方案一：Cloudflare Pages（推荐，完全免费）
1. 注册 cloudflare.com 账号
2. 进入 Workers & Pages → Pages → 直接上传
3. 将整个 `wang-stone` 文件夹拖拽上传
4. 自定义域名：在 Cloudflare 买域名（.com 约 ¥60/年），自动配置 DNS
5. 全球 CDN 加速，访问速度极快

### 方案二：GitHub Pages（完全免费）
1. 创建 GitHub 仓库，上传 `wang-stone` 文件夹
2. Settings → Pages → 选择分支 → 保存
3. 自定义域名：Settings → Custom domain
4. 访问地址：`你的用户名.github.io/仓库名`

### 方案三：国内云服务器（如果有备案）
1. 阿里云/腾讯云对象存储 OSS + CDN（约 ¥50-100/年流量费）
2. 上传整个文件夹到 OSS
3. 绑定已备案域名

## 🔧 留资表单配置

### 当前状态
表单在浏览器端拦截提交（演示模式），提交后显示成功提示。

### 接入免费表单服务（推荐 Formspree，50条/月免费）
1. 注册 formspree.io
2. 创建新 Form，获取 Form ID
3. 在 `index.html` 中找到这段注释代码，取消注释并替换 YOUR_FORM_ID：
```javascript
fetch('https://formspree.io/f/YOUR_FORM_ID', {
  method: 'POST',
  body: formData,
  headers: { 'Accept': 'application/json' }
});
```

### 替代方案：微信二维码收资
在表单下方放微信号/企业微信二维码，高净值业主更习惯微信沟通。

## 📱 内容分发策略

1. **小红书**：把改造前后对比图发笔记，标签 `#上海别墅翻新 #别墅石材 #翡翠湖岸`
2. **微信朋友圈**：发无人机航拍图 + 小区实景，建立圈层信任
3. **抖音**：施工过程短视频（不需要专业设备，手机实拍即可）
4. **所有内容都指向这个页面链接**

## 🎨 页面设计说明

- **色系**：暖米色基底 + 金色点缀，传递奢侈感
- **动效**：滚动渐入动画、悬停放大、平滑过渡
- **移动端优先**：在手机上效果完美
- **加载优化**：图片使用 lazy loading，不影响首屏速度

---

**总成本：域名约 ¥60-70/年，托管完全免费。**
